import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        /* write your code here
        byte x = 1;
        short y = 15000;
        int z = 100500;
        long w1 = 10000000;
        long w2 = 10000L;
        char C = 'O';
        */
        /*
        Scanner scanner = new Scanner(System.in);

        int a = scanner.nextInt();
        int b = scanner.nextInt();

        if (a > b) {
            System.out.println("Winner!");
        } else {

            System.out.println("Loser!");
        }
        */
        Scanner scanner = new Scanner(System.in);

        int a = scanner.nextInt();
        int b = scanner.nextInt();

        if (a > b) {
            System.out.println("The maximum is " + a);
        } else {

            System.out.println("The maximum is " + b);
        }



        /*double sum = a / (b*1.0);
        System.out.println("Sum: " + sum);

        boolean isWritten = false;
        boolean isRead = true;
        boolean result = isWritten && isRead;
        boolean orResult = isWritten || isRead;


        int x=5;
        int y =4;
        boolean isEqual = x == y;
        boolean isNotEqual = x != y;
        boolean isMore = x > y;
        boolean isMoreOrEqual = x >= y;
        boolean isLess = x < y;
        boolean isLessOrEqual = x <= y;

        boolean negation = !isEqual;

        if (isEqual){
            System.out.println(y);


        }
        */
    }

}
